# Copier ce fichier vers .env puis renseigner la vraie valeur :
#   cp frontend/.env.example frontend/.env
# Ne JAMAIS commiter le .env rempli.

# URL du backend (dev local par défaut)
VITE_API_URL=http://localhost:7080
